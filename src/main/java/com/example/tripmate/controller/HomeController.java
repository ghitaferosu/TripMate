package com.example.tripmate.controller;

import org.springframework.stereotype.Controller;

@Controller
public class HomeController {
    // Removed duplicate /dashboard mapping to avoid conflict with TripController
}
